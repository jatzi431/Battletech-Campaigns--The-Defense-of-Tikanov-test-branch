extends Button

#@onready var TurnManager = $TurnManager
#signal turn_button_pressed()

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	#var button = Button.new()
	#button.text = "End Turn"
	#button.pressed.connect(TurnManager._on_turn_button_pressed)
	#add_child(button)

#func _button_pressed():
	#
	#print("end turn")
